# Acceptance Criteria

The skill accepts only its documented read-only actions, validates a supplied cluster identifier before querying, does not modify cloud or Kubernetes resources, and returns a structured result with evidence, findings, and data gaps.
