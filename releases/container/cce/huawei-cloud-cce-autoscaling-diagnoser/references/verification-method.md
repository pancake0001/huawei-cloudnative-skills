# Verification Method

Verify that the target cluster resolves through `hcloud CCE ShowCluster`, then run the primary diagnostic action with a valid `region` and `cluster_id`. Confirm the result identifies collected evidence and explicitly reports unavailable data sources.
