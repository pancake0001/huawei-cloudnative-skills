# Workflow

1. 将用户意图转换成动作、对象、参数和验证标准。
2. 检查是否属于 R2/R3 高风险动作。
3. 第一次调用必须不带 `confirm=true`，获取预览或风险提示。
4. 输出预览结果、影响范围、回滚方式、执行后验证计划。
5. 等待用户明确确认，确认内容至少包含动作、对象、region、cluster_id。
6. 用户确认后才允许携带 `confirm=true`。
7. 执行后调用只读工具验证 Pod、Node、Workload、Events 或漏洞状态。

