# Workflow

1. 确认迁移目标：同区域、跨区域、多集群、混合云、版本升级或架构调整。
2. 盘点源集群基础信息、节点池、插件、网络模型和关键配置。
3. 盘点工作负载、Service、Ingress、PVC/PV、ConfigMap、Secret。
4. 盘点关联云资源：VPC、子网、安全组、ELB、EIP、EVS、SFS。
5. 建立依赖矩阵：入口流量、服务依赖、存储依赖、配置依赖、外部系统。
6. 设计迁移批次、验证点、回退策略和停机窗口。
7. 输出方案，不执行变更。

